# Postman Agent Skill - Environment Variables

# Required: Your Postman API key
# Get yours from: https://web.postman.co/settings/me/api-keys
POSTMAN_API_KEY=PMAK-your-key-here

# Recommended: Workspace ID to scope operations
# Find this in your workspace settings or URL
POSTMAN_WORKSPACE_ID=your-workspace-id-here

# Optional: Rate limit handling (seconds to wait)
POSTMAN_RATE_LIMIT_DELAY=60

# Optional: Maximum retry attempts
POSTMAN_MAX_RETRIES=3

# Optional: Request timeout (seconds)
POSTMAN_TIMEOUT=30

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Optional: Enable proxy (default is false to avoid "403 Forbidden" proxy errors)
# Only set to true if you specifically need to use a proxy
# POSTMAN_USE_PROXY=false
